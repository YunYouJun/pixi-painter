{
  "name": "pixi-painter",
  "type": "module",
  "version": "0.0.4-beta.0",
  "scripts": {
    "build": "unbuild",
    "dev": "unbuild --stub",
    "prepublishOnly": "npm run build",
    "release": "pnpm publish"
  },
  "devDependencies": {
    "unbuild": "^2.0.0"
  }
}
